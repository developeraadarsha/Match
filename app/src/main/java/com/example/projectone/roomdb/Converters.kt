package com.example.projectone.roomdb

import androidx.room.TypeConverter
import com.example.projectone.model.Results
import com.google.gson.reflect.TypeToken
import com.google.gson.Gson
import java.util.ArrayList

object Converters {

//    @TypeConverter
//    fun listToJson(value: List<Results>?) = Gson().toJson(value)
//
//    @TypeConverter
//    fun jsonToList(value: String) = Gson().fromJson(value, Array<Results>::class.java).toList()
}